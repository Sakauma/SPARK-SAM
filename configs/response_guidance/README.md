Each dataset-specific file points to the separately trained prompt estimator,
the official SAM2.1-Large checkpoint, and a train-split-only output cache.
The cache is supervision for model training and is never read during inference.
